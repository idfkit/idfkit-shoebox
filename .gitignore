node_modules/
dist/
build/
*.log
.env*
.DS_Store
Thumbs.db
*.tmp
*.swp
.vscode/
.idea/
public/energyplus/
public/schemas/
public/weather/

# scratch for scripts/build-rates.mjs
.rates-cache/

# scratch for scripts/build-tm59.mjs. The CSV it reads is an independent
# transcription of TM59:2026 Appendix E, which is a purchased document, so the
# input stays out of the repository exactly as the tables themselves do.
.tm59-cache/

# Throwaway verification harnesses (see CLAUDE.md, "Verifying changes").
harness/
.harness/

# Feature worktrees, each its own checkout of this repository.
.claude/worktrees/

# The assess extension and its drafts are local tooling, installed per checkout
# rather than shared: `.specify/extensions.yml` records what is installed here,
# and an assessment is a working note about a feature that has not been shaped
# yet. Neither belongs in a feature's pull request.
.claude/skills/speckit-assess-*/
.specify/extensions.yml
.specify/extensions/
.specify/assessments/
