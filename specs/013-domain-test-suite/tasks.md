---

description: "Task list for 013-domain-test-suite"
---

# Tasks: An executing verification suite for the model

**Input**: Design documents from `/specs/013-domain-test-suite/`

**Prerequisites**: [plan.md](./plan.md), [spec.md](./spec.md), [research.md](./research.md), [data-model.md](./data-model.md), [contracts/](./contracts/)

**Tests**: This feature *is* the tests. Every task below that writes a check is an implementation task, not an optional test layer, so there is no separate "tests first" section — the checks are the deliverable.

**Organization**: Tasks are grouped by user story. Stories run in priority order: US1, US2, US3, US6 (P1), then US4, US5 (P2), then US7 (P3).

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: Which user story this task belongs to (US1–US7)
- Every task names the exact file it touches

## Path Conventions

Single project. `src/` and `index.html` at the repository root are the subject; `tests/` is a sibling tree that imports `src/` directly with no build step between them (plan.md, Structure Decision). `eslint-rules/` and `scripts/` sit beside them.

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: The tooling and the tree, before any check exists

- [ ] T001 Add `eslint`, `prettier`, `eslint-config-prettier` and `jsdom` to `devDependencies` in `package.json`, leaving `dependencies` untouched at its four `@idfkit/*` pins
- [ ] T002 [P] Create the test tree `tests/{support,fixtures/weather,fixtures/run,goldens,mutations,fast,model,engine}/` with a `.gitkeep` in each directory that is still empty
- [ ] T003 Add the `verify`, `verify:fast`, `verify:model`, `verify:engine`, `verify:proof`, `lint`, `format` and `format:check` scripts to `package.json` exactly as the command table in `specs/013-domain-test-suite/contracts/runner.md` states
- [ ] T004 [P] Create `.prettierrc.json` at the repository root
- [ ] T005 [P] Create `.prettierignore` listing `src/rates.data.js` and `src/tm59.data.js`, with a comment saying they are generated by `scripts/build-rates.mjs` and `scripts/build-tm59.mjs` and not authored
- [ ] T006 [P] Create `eslint.config.js` as a flat-config skeleton wiring `eslint-config-prettier` and the local plugin from `eslint-rules/`, covering `src/`, `scripts/`, `tests/` and `infra/`
- [ ] T007 [P] Create `eslint-rules/index.js` exporting the local plugin object with an empty rules map, documenting the `meta.docs.invariant` convention from research D-10
- [ ] T008 [P] Update `.gitignore` so `tests/` is tracked while `harness/` stays ignored, with a comment recording why the two are different things (plan.md: what a harness proved stops being thrown away)

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: The declarations and support modules every tier imports. Types are frozen classes validating in their constructors, per `data-model.md` and the constitution's tenth gate.

**⚠️ CRITICAL**: No user story work can begin until this phase is complete

- [ ] T009 [P] Create `tests/support/nonet.js`: an undici global dispatcher that throws on any request, naming the host reached for (FR-005)
- [ ] T010 [P] Create `tests/support/tiers.js`: the frozen `Tier` class (`id`, `glob`, `budget`, `prerequisites`) validating in its constructor, plus the four tier declarations at budgets 60 s, 300 s, 900 s and none for `proof`
- [ ] T011 [P] Create `tests/support/staging.js`: one probe per prerequisite with its remedy sentence, for all three tiers, worded as `contracts/runner.md` § "Refusing a tier" shows
- [ ] T012 [P] Create `tests/support/schema.js`: load `localBundle()` from `@idfkit/schemas/node` with the full version string `load('26.1.0')`, once per process, asserting the bundle's version rather than assuming it
- [ ] T013 [P] Create `tests/support/dom.js`: set the `jsdom` globals then `await import()` the module under test, per research D-05
- [ ] T014 [P] Create `tests/support/register.js` exporting `covers(id, name, fn)`, which declares `test(`${id}: ${name}`, fn)` and holds no runtime registry (`contracts/registry.md`)
- [ ] T015 [P] Create `tests/support/golden.js`: `matchesGolden(id, text)` comparing byte for byte against `tests/goldens/<id>.idf`, printing a unified diff capped at 200 lines, writing instead under `UPDATE_GOLDENS=1`, and refusing `UPDATE_GOLDENS=1` when `CI` is set
- [ ] T016 [P] Create `tests/support/eplus-child.mjs`: set `global.Module = { noInitialRun: true, locateFile }`, `require` `public/energyplus/energyplus.js`, call `callMain(['-d', outDir, '-w', epw, idf])` exactly once, exit — under the same network-refusing dispatcher as its parent
- [ ] T017 Create `tests/support/engine.js` exporting `runEnergyPlus({ idf, epw, outDir })`, forking one child per run and pooling at `os.availableParallelism()` capped at 6 (depends on T016)
- [ ] T018 Create the `DeskPosition` class in `tests/support/desk.js` with the constructor validation `data-model.md` specifies: every `params` key exists in `DEFAULT_PARAMETERS`, every value is a scalar, every `patching` id is a declared channel
- [ ] T019 Declare the nineteen representative desk positions in `tests/support/desk.js` — `baseline`, `bare`, twelve `engaged-<channel>`, `full`, `shrunk`, `built-small`, `sealed`, `north-turned` — each with its `covers`, `simulated` and `environment` (depends on T018)
- [ ] T020 Implement `buildDocument(position)` in `tests/support/desk.js`: overlay `position.params` on `DEFAULT_PARAMETERS`, build the patch bay, call the real `applyModel` from `src/model.js`, return `{ doc, text }` (depends on T012, T019)
- [ ] T021 [P] Create the `Fixture` class and the manifest in `tests/fixtures/manifest.js`, refusing to declare a fixture with no `origin` (data-model.md)
- [ ] T022 [P] Vendor one complete TMYx EPW (gzipped) and its DDY into `tests/fixtures/weather/`, with manifest entries recording origin, licence and date taken
- [ ] T023 [P] Vendor a hand-trimmed DDY carrying a literal `N` in a numeric field into `tests/fixtures/weather/`, with its manifest entry saying which invariant needs it
- [ ] T024 [P] Vendor a hand-trimmed DDY with no `DB=>MWB` cooling day into `tests/fixtures/weather/`, with its manifest entry naming the refusal scenario it serves
- [ ] T025 [P] Vendor trimmed `.eso`, `.mtr`, `.rdd` and `eplustbl.htm` from one real run into `tests/fixtures/run/`, so `readings.js`, `bill.js` and `tm59.js` are checkable in the `fast` tier with no engine
- [ ] T026 [P] Create the `Invariant` class in `tests/invariants.js` with the constructor validation from `data-model.md`: id pattern and uniqueness, non-empty quote, `unexecutable` requiring a reason and forbidding a tier, every other evidence class requiring a tier and forbidding a reason
- [ ] T027 Declare all twenty-six invariants in `tests/invariants.js`, each quoting the opening clause of its `CLAUDE.md` bullet verbatim, with its evidence class (`executed`, or `structural` for the five whose subject is in `src/main.js` per research D-04) and its tier — the boundary being the invariant rather than the file (FR-037, depends on T026)
- [ ] T028 [P] Create the `Gate` class and declare all ten constitutional gates in `tests/gates.js`, marking gates 7 and 9 `human` with their reasons and gates 8 and 10 as partially executed, per the plan's gate table
- [ ] T029 Create `scripts/verify.mjs`: the driver that runs tiers in order, measures elapsed time against each budget, prints the per-tier verdicts and one overall verdict, and exits `0`/`1`/`2`/`3` per `contracts/runner.md` (depends on T010, T011)

**Checkpoint**: declarations load and throw on malformed input; `buildDocument` returns a document; nothing is asserted yet

---

## Phase 3: User Story 1 - Know a change is sound before opening the page (Priority: P1) 🎯 MVP

**Goal**: One documented command, one verdict, three tiers with budgets, a refusal that never looks like a pass, and static analysis inside the same verdict.

**Independent Test**: On a fresh clone with setup done, `npm run verify` reports a pass and exits `0`; breaking one recorded invariant makes it report a failure naming that invariant; `rm -rf public/energyplus && npm run verify:engine` exits `2` naming the missing file and the command that stages it.

- [ ] T030 [US1] Implement tier sequencing and the single overall verdict in `scripts/verify.mjs`, with `fast` then `model` then `engine`, and no status meaning "passed, with checks skipped" (FR-001)
- [ ] T031 [US1] Implement tier refusal in `scripts/verify.mjs`: a failed probe prints the two-line message to stderr, runs no check in that tier, reports nothing as passing, and makes the overall verdict `fail` with exit `2` (FR-004)
- [ ] T032 [US1] Implement budget measurement in `scripts/verify.mjs`: write the elapsed time into every verdict whether or not it held, and exit `3` when a tier passes but exceeds its stated budget (FR-002)
- [ ] T033 [US1] Wire the runner flags in `scripts/verify.mjs`: `--experimental-test-isolation=process`, `--test-reporter=spec` locally and `tap` under `CI`, `TZ=UTC` and `LC_ALL=C` for every tier
- [ ] T034 [US1] Wire `--experimental-test-coverage` with `--test-coverage-include='src/**'` in `scripts/verify.mjs`, printing the figure and passing no threshold flag (FR-031)
- [ ] T035 [US1] Put `lint` and `format:check` *inside* `verify:fast` in `package.json`, not only beside it, so a lint finding or an unformatted file lands in the same verdict as every other check (FR-033)
- [ ] T036 [P] [US1] Write `tests/fast/no-skip.test.js`: assert that `skip`, `todo` and `only` appear nowhere under `tests/`, and that the coverage threshold flags appear nowhere in `package.json` or the workflows — the suite offers no way past a failing check (FR-024, SC-011, SC-018)
- [ ] T037 [P] [US1] Write `tests/fast/nonet.test.js`: assert the dispatcher throws on an attempted request and that its message names the host (FR-005)
- [ ] T038 [P] [US1] Write `tests/fast/dependencies.test.js`: assert `package.json`'s `dependencies` holds only `@idfkit/*` entries and that the four new packages are under `devDependencies` (FR-040, SC-014)
- [ ] T039 [US1] Write `tests/fast/staging.test.js`: assert each tier's probes resolve or produce the exact refusal wording, including the `model` tier refusing a schema bundle that loads at a version other than `26.1.0`
- [ ] T040 [US1] Run quickstart scenarios 1, 3 and 4 from `specs/013-domain-test-suite/quickstart.md` and record the measured elapsed times against SC-005 in the run's output

**Checkpoint**: `npm run verify` and `npm run verify:fast` both work end to end and refuse honestly. This is the MVP — the runner exists and tells the truth about what it ran.

---

## Phase 4: User Story 2 - Every recorded invariant fails loudly when broken (Priority: P1)

**Goal**: The prose keeps exactly one statement of each rule, the register indexes it, a check claims each entry, and a declared mutation proves each check turns red for its own invariant.

**Independent Test**: `npm run verify:proof` applies twenty-six mutations and each turns its tier red naming its own invariant. Adding a 27th bullet to `CLAUDE.md` makes `verify:fast` fail naming the unclaimed bullet.

### The register and the bijection

- [ ] T041 [US2] Implement the static discovery pass in `tests/support/register.js`: walk `tests/**/*.test.js` and `eslint-rules/*.js` collecting every string literal passed as `covers`' first argument and every `meta.docs.invariant`
- [ ] T042 [US2] Write `tests/fast/register.test.js` asserting the four register directions from `contracts/registry.md`: every non-unexecutable `Invariant` claimed, every claimed id resolving, every `unexecutable` entry carrying a reason and claimed by nothing, every `Gate` claimed or `human` with a reason (FR-006, FR-008)
- [ ] T043 [US2] Add the bijection against the prose to `tests/fast/register.test.js`: extract the top-level bullets of `CLAUDE.md` § "Invariants that fail quietly" and assert one-to-one correspondence by `Invariant.quote`, with a distinct named message for each of the four failure modes (FR-026, FR-030)
- [ ] T044 [US2] Add the same bijection against `.specify/memory/constitution.md`'s ten numbered gates to `tests/fast/register.test.js` (FR-008)
- [ ] T045 [US2] Implement the coverage record in `scripts/verify.mjs`: compute it from the register, the discovery pass and the last recorded proof run; print it and write it to the CI job summary; do not commit it (data-model.md `CoverageRecord`)
- [ ] T046 [US2] Add the two Principle VII claims — readable at 390 px, nothing on hover — to the coverage record as `unexecutable` rows with the reasons from research D-13, in `tests/gates.js`

### The custom lint rules (the structural half, research D-04 and D-10)

- [ ] T047 [P] [US2] Write `eslint-rules/covers-literal-id.js`: `covers`' first argument must be a string literal, because the discovery pass reads source rather than running it
- [ ] T048 [P] [US2] Write `eslint-rules/frame-flag-inside-raf.js` for the hidden-tab invariant — the frame flag must be cleared inside the `requestAnimationFrame` callback
- [ ] T049 [P] [US2] Write `eslint-rules/lettered-cache-keys-system.js` for the invariant that a cache whose key cannot see the unit system holds a converted string past the switch
- [ ] T050 [P] [US2] Write `eslint-rules/no-reletter-geometry.js` asserting `reletterSheet` never calls `applyGeometry`
- [ ] T051 [P] [US2] Write `eslint-rules/difference-not-format.js` asserting a subtraction of readings goes through `Reading.change`, never `Reading.format`
- [ ] T052 [P] [US2] Write `eslint-rules/no-dataset-assign.js` for the getter-only `dataset` invariant
- [ ] T053 [P] [US2] Write `eslint-rules/no-labels-in-issue-url.js`, and assert the new-issue address stays under 5,500 characters where `handoff()` builds it
- [ ] T054 [P] [US2] Write `eslint-rules/no-per-surface-output.js` for the per-surface output variable invariant, enforcing Principle VI's own clause
- [ ] T055 [P] [US2] Write `eslint-rules/suffix-not-unit-apart-from-figure.js` asserting `suffixIn` rather than `unitIn` wherever a unit is lettered apart from its figure
- [ ] T056 [US2] Give every rule in `eslint-rules/` a `meta.docs.invariant` id and a message repeating the id and the section of `CLAUDE.md` it is written in, then register them all in `eslint-rules/index.js` (depends on T047–T055)
- [ ] T057 [P] [US2] Write `tests/fast/stylesheet.test.js`: parse the stylesheet in `index.html` and assert every class that sets or unsets `display` and is toggled by `hidden` has its own `[hidden]` twin — the one static check whose subject is not JavaScript (research D-10)

### The proof

- [ ] T058 [US2] Create the `Mutation` class in `tests/mutations/index.js` with the validation from `data-model.md`: `find` must occur exactly once in `file`, and `expect` may only be `'fails'`
- [ ] T059 [US2] Declare one mutation per invariant in `tests/mutations/`, twenty-six in all, each the breakage its `CLAUDE.md` bullet actually warns about (depends on T058)
- [ ] T060 [US2] Implement `verify:proof` in `scripts/verify.mjs`: copy the checkout to a scratch directory, apply one mutation, run the tier owning that invariant's check, assert the run fails *and* that the output names that invariant's id, restore, repeat — reporting a mutation that turns the suite red through another check as **unproved**, never as a pass (FR-007, SC-003)
- [ ] T061 [US2] Record the proof run's date and result into the coverage record from `scripts/verify.mjs` (depends on T045, T060)

**Checkpoint**: recording a rule and enforcing it are one act. US2 is independently demonstrable via `npm run verify:proof`.

---

## Phase 5: User Story 3 - Verify the building, not just the code (Priority: P1)

**Goal**: Every representative position is a real, runnable building; the engine's own report is read rather than trusted; physics moves the way physics moves; a model meant to fail fails for its stated reason.

**Independent Test**: `npm run verify:model && npm run verify:engine` on a machine with no EnergyPlus installed — nineteen positions validated, the simulated subset run, no fatal, no "requested but not generated", every output name confirmed against the `.rdd`.

- [ ] T062 [US3] Write `tests/model/validate.test.js`: schema-validate every one of the nineteen positions against the loaded `26.1.0` bundle and pass the integrity check, before any simulation, naming the position and the object on a failure (FR-016, SC-008)
- [ ] T063 [US3] Write `tests/engine/run.test.js` running the simulated subset through `runEnergyPlus` and asserting each completes and that `eplusout.err` carries no `** Fatal **` (FR-017)
- [ ] T064 [US3] Add the unmet-request scan to `tests/engine/run.test.js`: `eplusout.err` scanned for `requested but not generated` (FR-017)
- [ ] T065 [US3] Write `tests/engine/rdd.test.js`: confirm every `Output:Variable` in each document is present in that run's `eplusout.rdd`, and print the count of unconfirmed requested variables as `0` (FR-018, SC-009)
- [ ] T066 [US3] Write `tests/engine/isolation.test.js`: run two different models and assert the second's ESO is the second model's, so the non-re-entrant entry point cannot grade one against the other (FR-021)
- [ ] T067 [US3] Create the `Expectation` class in `tests/engine/expectations.js` with the validation from `data-model.md`: ordered positive band, non-empty `because`, `from` naming a declared position, `channel` naming one that reaches the IDF
- [ ] T068 [US3] Declare at least sixteen expectations in `tests/engine/expectations.js`, one per channel that reaches the model, each with its direction, band and physical reasoning (depends on T067, SC-010)
- [ ] T069 [US3] Implement `expects(id)` in `tests/engine/expectations.js`: run the basis and the moved position as two children, read the named reading through the real `src/readings.js`, assert direction and band, and print basis, moved, fractional change, band and `because` on a failure (FR-019, FR-025)
- [ ] T070 [US3] Create the `Refusal` class and declare the refusal scenarios in `tests/engine/refusals.js`, covering all three shapes — `requires`, `throw` and `engine` (FR-020)
- [ ] T071 [US3] Implement `refuses(id)` in `tests/engine/refusals.js`, asserting the refusal happens at the declared place and its message contains the declared `naming` substring, so a scenario failing for a different reason is a failure of the suite (FR-014, FR-020)
- [ ] T072 [US3] Add the `sealed` position's warmup-convergence scenario to `tests/engine/refusals.js` as a `refusedBy: 'engine'` refusal, asserted as physics rather than reported as a broken suite
- [ ] T073 [P] [US3] Write `tests/fast/readings.test.js` reading the vendored `.eso` in `tests/fixtures/run/` through the real `src/readings.js` — asserting TEDI/CEDI against their published definitions and that a missing System meter omits its row rather than zeroing it
- [ ] T074 [P] [US3] Write `tests/fast/bill.test.js` reading the vendored `.mtr` through the real `src/bill.js`, going *through* `meterName()`'s recovery of the names `parseMTR` mis-parses rather than around it, and asserting per-m² only on a whole year
- [ ] T075 [US3] Write `tests/engine/glazing.test.js` reading U-factor and SHGC from `eplustbl.htm` by **column head**, from the `WINDOW_CONSTRUCTION` row, never the area-weighted "Total or Average" row
- [ ] T076 [P] [US3] Write `tests/fast/tm59.test.js` over the vendored run fixtures: the running mean seeded 23–29 April at divisor 3.8, `roundDT` rounding half-up, `occupiedFloor(params)` rather than `> 0`, design days excluded, and the 153-day period
- [ ] T077 [P] [US3] Write `tests/fast/epw.test.js` over the vendored DDY carrying a literal `N`, asserting every field the schema types numeric is checked rather than assumed, and over the DDY with no `DB=>MWB` cooling day

**Checkpoint**: the suite verifies a building, not just the code around it. SC-007 is demonstrated the moment this runs on a machine with no EnergyPlus.

---

## Phase 6: User Story 6 - The check runs itself, on every change (Priority: P1)

**Goal**: The verdict is already there when a maintainer opens the change, produced by the project rather than by the author's good intentions.

**Independent Test**: Open a pull request that breaks a recorded invariant; `verify-fast` and `verify-full` report against it before a human looks.

- [ ] T078 [US6] Add the `verify-fast` job to `.github/workflows/check.yml` running `npm ci && npm run verify:fast` on `pull_request` and on `push` to `main`, beside the existing `consumer-register` job, so the verdict is visible on the change before review (FR-023)
- [ ] T079 [US6] Add the `verify-full` job to `.github/workflows/check.yml` running `npm ci && npm run predev && npm run verify:model && npm run verify:engine`, staging the engine on a runner with no EnergyPlus installed (SC-007)
- [ ] T080 [US6] Write the coverage record and the line-coverage figure into the job summary from `verify-full` in `.github/workflows/check.yml`, comparing neither with a threshold (FR-031, SC-018)
- [ ] T081 [US6] Assert `UPDATE_GOLDENS=1` is refused when `CI` is set, in `tests/support/golden.js`, and add a check for it in `tests/model/golden.test.js` (FR-022)
- [ ] T082 [US6] Create `.github/workflows/proof.yml` running `npm run verify:proof` weekly and on `workflow_dispatch`, writing its result into the job summary (research D-11)
- [ ] T083 [US6] Record the branch-protection hand-off in `specs/013-domain-test-suite/plan.md` § Sequencing as delivered-pending: marking `verify-fast` and `verify-full` required is a repository setting this change cannot make, and required is what "reported as blocking" means (FR-024)

**Checkpoint**: every proposed change gets a verdict before review (SC-006).

---

## Phase 7: User Story 4 - The document the engine gets does not drift (Priority: P2)

**Goal**: The three byte-identity claims the project already makes, plus determinism, plus goldens a reviewer can hand to EnergyPlus.

**Independent Test**: `npm run verify:model` — idempotence at every position, `shrunk` identical to `built-small`, lean-then-sheet identical to always-sheet, every position matching its golden. Change an applier and the failure is a unified diff of the IDF.

- [ ] T084 [US4] Write `tests/model/idempotence.test.js`: apply `applyModel` three times over one document at every position and assert `text` is identical after each application (FR-009)
- [ ] T085 [US4] Write `tests/model/shrink.test.js`: build `shrunk` and compare byte for byte against `built-small`, so a surviving orphaned object from a sweeping channel fails by name (FR-009)
- [ ] T086 [US4] Write `tests/model/reporting.test.js`: set reporting lean, apply, set it full, apply, and compare against full throughout — what `syncReporting`'s clear-and-rewrite exists for (FR-009)
- [ ] T087 [US4] Write `tests/model/determinism.test.js`: build every position under `TZ=UTC`/`LC_ALL=C` and again under `TZ=Pacific/Kiritimati` with a non-English locale, in a fresh process, and compare bytes (FR-010)
- [ ] T088 [US4] Generate the nineteen goldens into `tests/goldens/<id>.idf` under `UPDATE_GOLDENS=1` and commit them as real, loadable IDF files (research D-07)
- [ ] T089 [US4] Write `tests/model/golden.test.js` asserting every position matches its golden through `matchesGolden`, printing a unified diff capped at 200 lines and naming the file on a difference (FR-022)
- [ ] T090 [US4] Add a check to `tests/model/determinism.test.js` that `Math.random`, `Date.now`, `new Date()` and locale-sensitive formatting reach no module on the applier path, asserting Principle II's clause structurally as well as behaviourally

**Checkpoint**: an intended change to what the model writes is a readable diff, accepted deliberately. Unintended drift is caught.

---

## Phase 8: User Story 5 - The link and the lettering are exercised exhaustively (Priority: P2)

**Goal**: The two combinatorial subsystems checked by enumeration rather than by sampling — which is what a machine is for and what a human harness will not do.

**Independent Test**: `npm run verify:fast -- --test-name-pattern='codec|lettering'` — every declared key round-tripped, every malformed class refused whole, every face lettered in both systems.

- [ ] T091 [US5] Write `tests/fast/codec.test.js` round-tripping every declared control key through `src/permalink.js` at both extremes and several interior stops, for all nine kinds including those carrying canonical text (FR-011)
- [ ] T092 [US5] Add the malformed-input classes to `tests/fast/codec.test.js`, asserting each is refused whole with a stated reason and that no partial state is adopted (FR-011)
- [ ] T093 [US5] Add the migration-chain check to `tests/fast/codec.test.js`, resolving every version in `DEFAULTS_BY_VERSION` through `MIGRATIONS` — asserted even though the chain is currently empty, so the first migration is not the first time it is exercised (FR-011)
- [ ] T094 [US5] Add the reserved-key check to `tests/fast/codec.test.js` (`in`, `out`, `stn`, `win`, `at`, `sty`, `sv` read above `readValue`) and the `URLSearchParams` escaping claim that only `*`, `.`, `-` and `_` survive unescaped
- [ ] T095 [US5] Write `tests/fast/lettering.test.js` asserting `format(v).endsWith(unitNow)` over all 87 faces in **both** unit systems, through `suffixIn` rather than `unitIn`, since in SI alone the two agree by accident (FR-012)
- [ ] T096 [US5] Add the difference-kind checks to `tests/fast/lettering.test.js`: a temperature change, a span along a face and a change in a reading each lettered as a difference, asserting `+1 °C` does not read `+33.8 °F` and a 5 K span does not read `41 °F` (FR-012)
- [ ] T097 [US5] Add the `deltaKindOf` enumeration to `tests/fast/lettering.test.js`, asked of every reading rather than only of temperatures, since it returns a zero-offset kind unchanged
- [ ] T098 [US5] Write `tests/fast/caches.test.js` asserting `chooserDrawn` and `noteCache` keys carry `system()`, checked behaviourally against `src/survey.js` and `src/tm59.js` where the subject is importable, beside the structural rule from T049 (FR-013)
- [ ] T099 [P] [US5] Write `tests/fast/aria.test.js` under `jsdom`: mount the console through `src/console.js`, change the unit system, and assert the Study buttons' sweep-range `aria-label`s re-letter through `studySweeps` rather than standing in the system they were built in
- [ ] T100 [P] [US5] Write `tests/fast/declarations.test.js` asserting the load-time declaration invariants as checks rather than only as page-load throws: `readLandmarks`' four rules, `assertKinds`, `assertReachable`, `assertHideable`, `TM59_SPACES === PROFILE_IDS`, `Coverage`'s sum, `UNTOUCHABLE`, and the copy budgets in `src/copy.js` (FR-015)

**Checkpoint**: the link and the lettering are covered by enumeration, so a new key or kind cannot slip through untested.

---

## Phase 9: User Story 7 - A contributor knows where a new check goes (Priority: P3)

**Goal**: Adding to the suite is a small obvious act, and where the project already enumerates something the suite enumerates it too, so there is nothing to remember.

**Independent Test**: Add a control to `src/controls.js` and run `npm run verify:fast` — a failure names the new key and what it now owes the suite.

- [ ] T101 [US7] Write `tests/fast/enumeration.test.js` asserting every `CHANNELS` member has a `DeskPosition` covering it and — unless `prices: true` — an `Expectation` (FR-026)
- [ ] T102 [US7] Add the control-key and `KINDS` enumeration to `tests/fast/enumeration.test.js`: every key claimed by a codec round trip, every `Ruled` face by a lettering check in both systems, every kind by a lettering check and a difference check where `deltaKindOf` differs
- [ ] T103 [US7] Add the remaining declaration rosters to `tests/fast/enumeration.test.js`: landmark lists, `PRESETS` and their `Target`s, and `src/copy.js`'s `Budget` roster with its `asserted: false` members listed rather than silently passed
- [ ] T104 [US7] Write `tests/fast/kinds.test.js` asserting the four gates a new control kind must pass: `buildControl` draws it, `readValue` is taught above the numeric regex, key ownership is taught in all five places, and `assertHideable` is passed its `when`
- [ ] T105 [US7] Add the "Where a new check goes" table from `specs/013-domain-test-suite/quickstart.md` to `CLAUDE.md` § Verifying changes, so the answer lives where a contributor already looks (FR-027)
- [ ] T106 [US7] Record in `.specify/memory/constitution.md` the practice that a bug fix carries a check failing before the fix and passing after it, as part of the amended Development Workflow section (FR-028, SC-013)

**Checkpoint**: the suite tells a contributor what they owe it, by name, rather than relying on them to remember.

---

## Phase 10: Style, adopted and enforced

**Purpose**: FR-032–FR-036. Sequenced as the plan's second change: the rule set is reviewed before the sweep, and the sweep is the final commit of the same pull request — a rule set without its sweep leaves `main` red, and a sweep without its rule set is a diff nobody could justify.

**Note**: the custom rules land earlier, in Phase 4, because US2's coverage of the five `src/main.js` invariants depends on them. What remains here is the stock rule set, Prettier, the reasons audit and the mechanical sweep.

- [ ] T107 Select the stock ESLint rule set in `eslint.config.js` against this project's own recorded failure classes rather than adopting a preset wholesale (FR-034)
- [ ] T108 Turn off every rule that would fight a house convention in `eslint.config.js` — prose comments carrying the measurement that forced a decision, declarations that throw at module load, deliberate byte-level assembly of a document — each with its reason on the line beside it (FR-034)
- [ ] T109 Write `tests/fast/style-reasons.test.js` asserting every rule explicitly set to `'off'` or `0` in `eslint.config.js`, and every `eslint-disable` comment in `src/`, carries a reason (SC-017)
- [ ] T110 Run `npm run lint` over `src/`, `scripts/`, `tests/` and `infra/` and fix every finding, in the files each finding names, until it reports zero (SC-015)
- [ ] T111 Run `npm run format` as the mechanical sweep across `src/`, `scripts/`, `tests/`, `infra/` and the repository root, honouring `.prettierignore`, as the final commit of this change (FR-035)
- [ ] T112 Assert the sweep moved nothing: `npm run verify:model` passes and `git diff --stat main -- tests/goldens/` is empty, proving a diff touching every file changed no byte of any document the engine receives (FR-036, SC-016)

**Checkpoint**: a lint finding or an unformatted file is a failure like any other, and the sweep is proved harmless by the suite built in Phase 3.

---

## Phase 11: Polish, governance and cross-cutting concerns

**Purpose**: Bring the governing documents into agreement with the practice, in the same change (FR-029)

- [ ] T113 Amend `.specify/memory/constitution.md` from 1.0.1 to 1.1.0: retire the Development Workflow section's opening sentence, restate all ten gates with the tier that executes each, and name the verification tooling in Principle V's exemption sentence beside Vite and the AWS SDK
- [ ] T114 Add the amendment block to the Sync Impact Report comment at the head of `.specify/memory/constitution.md`, in the existing style, recording the MINOR bump and its rationale (depends on T113)
- [ ] T115 [P] Remove "There is no test runner and no linter" from `CLAUDE.md` and replace its § Verifying changes with a pointer to the commands in `specs/013-domain-test-suite/quickstart.md` (FR-029)
- [ ] T116 [P] Remove the same sentence from `docs/design-notes.md` and rewrite its § Verifying changes as the long-form record of why the suite is shaped this way (FR-029)
- [ ] T117 [P] Record in `docs/design-notes.md` the follow-up the plan deliberately excludes: moving `renderSurveySoon`, `renderPullSoon`, `noteCache`, `chooserDrawn` and `reletterSheet` out of `src/main.js` would upgrade five coverage rows from `structural` to `executed` (research D-04) — and record there that interface behaviour beyond the recorded invariants is out of this feature's scope rather than assumed closed (FR-038, FR-039)
- [ ] T118 [P] Write `tests/fast/fixtures.test.js` asserting no fixture under `tests/fixtures/` carries TM59 table content, compared against `src/tm59.data.js`'s generated values, and that every fixture has an `origin` in the manifest (FR-041)
- [ ] T119 Update `.github/workflows/check.yml`'s header comment, which currently states this repository has no test runner and no linter and that it therefore had no check workflow at all
- [ ] T120 Run `scripts/verify.mjs` ten consecutive times via `npm run verify` on an unchanged checkout and assert identical verdicts each time, recording the result (SC-012)
- [ ] T121 Walk all fifteen validation scenarios in `specs/013-domain-test-suite/quickstart.md` end to end on a fresh clone with no EnergyPlus installed, and record the measured times against SC-001 and SC-005 (FR-003)
- [ ] T122 Produce the final coverage record from `scripts/verify.mjs` and confirm it reads 26 invariants declared, 21 executed, 5 structural, 0 unexecutable, 10 gates with 8 executed and 2 human, and the two Principle VII claims with their reasons, with the uncovered interface gap stated rather than omitted (FR-039, SC-002, SC-004)

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: no dependencies
- **Foundational (Phase 2)**: depends on Setup — **blocks every user story**
- **US1 (Phase 3)**: depends on Foundational. Nothing depends on it, but it is the MVP
- **US2 (Phase 4)**: depends on Foundational. T041–T046 depend on checks existing to discover, so the register's bijection is written here and re-run as later phases add checks
- **US3 (Phase 5)**: depends on Foundational, specifically T017 (`runEnergyPlus`), T020 (`buildDocument`) and T025 (run fixtures)
- **US6 (Phase 6)**: depends on US1 (there must be a command for CI to run); T081 depends on T015
- **US4 (Phase 7)**: depends on Foundational T015 and T020
- **US5 (Phase 8)**: depends on Foundational only — the fast tier needs no engine and no schema
- **US7 (Phase 9)**: depends on US5 (its enumeration checks assert that the codec and lettering checks exist)
- **Style (Phase 10)**: depends on US2 (the custom rules) and on US4 (the goldens that prove the sweep moved nothing)
- **Polish (Phase 11)**: depends on everything it documents

### Within phases

- T019 → T018; T020 → T012, T019; T017 → T016; T027 → T026; T029 → T010, T011
- T030–T035 all touch `scripts/verify.mjs` or `package.json` and are therefore **sequential**, not parallel
- T056 → T047–T055; T059 → T058; T060 → T059; T061 → T045, T060
- T068 → T067; T069 → T068; T089 → T088; T114 → T113

### Parallel Opportunities

- **Phase 1**: T002, T004, T005, T006, T007, T008 all touch different files
- **Phase 2**: the support modules T009–T016 are eight independent files; the fixtures T021–T025 are five more; T026 and T028 are two more. This phase parallelises widely
- **Phase 4**: the nine custom rules T047–T055 are nine separate files under `eslint-rules/`
- **Phase 5**: T073, T074, T076 and T077 are independent fast-tier files over vendored fixtures and need no engine — they can be written while the engine tier is still being built
- **Phase 11**: T115, T116, T117 and T118 touch four different files

### Cross-story parallelism

Once Phase 2 completes, **US5 (Phase 8) is entirely independent of the engine** and can be built alongside US3 by a second contributor. US1 and US2 share `scripts/verify.mjs` and should not be split across people.

---

## Parallel Example: Phase 2 Foundational

```bash
# The support modules — eight independent files:
Task: "Create tests/support/nonet.js"
Task: "Create tests/support/tiers.js"
Task: "Create tests/support/staging.js"
Task: "Create tests/support/schema.js"
Task: "Create tests/support/dom.js"
Task: "Create tests/support/register.js"
Task: "Create tests/support/golden.js"
Task: "Create tests/support/eplus-child.mjs"

# The fixtures — five more, in parallel with the above:
Task: "Create tests/fixtures/manifest.js"
Task: "Vendor the TMYx EPW and DDY into tests/fixtures/weather/"
Task: "Vendor the DDY carrying a literal N"
Task: "Vendor the DDY with no DB=>MWB cooling day"
Task: "Vendor the recorded run outputs into tests/fixtures/run/"
```

---

## Implementation Strategy

### MVP (US1 only)

1. Phase 1 Setup → Phase 2 Foundational → Phase 3 US1
2. **STOP and VALIDATE**: `npm run verify` reports a verdict and exits `0`; `rm -rf public/energyplus && npm run verify:engine` exits `2` naming what is missing
3. At this point the repository has a test runner that tells the truth about what it ran — which is the headline of the whole feature — even though it asserts little about the model yet

### Incremental delivery

1. **US1** → a command and an honest verdict (MVP)
2. **US2** → the 26 invariants claimed, the prose indexed, the mutations proving each check. This is where SC-003 lands and where the feature stops being infrastructure and starts being a guarantee
3. **US3** → the engine tier: real schema, real runs, real physics. SC-007, SC-008, SC-009, SC-010
4. **US6** → CI, so the verdict precedes review. SC-006
5. **US4** → goldens and byte-identity, which also unblocks Phase 10's proof that the sweep moved nothing
6. **US5** → the combinatorial subsystems, exhaustively
7. **US7** → enumeration, so the suite stays alive past this feature
8. **Phase 10** → style adopted, then swept, as the plan's second change
9. **Phase 11** → the constitution and the documents brought into agreement

### Parallel team strategy

After Phase 2: one contributor takes US1 → US2 → US6 (they share `scripts/verify.mjs` and the register); a second takes US3 → US4 (they share the engine and the goldens); a third takes US5 → US7 (fast tier only, no engine, no schema). Phase 10 and Phase 11 are single-threaded and come last.

---

## Notes

- Every task names a file. Checks are the deliverable here, so there is no separate test layer to write first
- `[P]` means a different file and no incomplete dependency
- The three tasks that are *removals* rather than additions — T115, T116, T119 — matter as much as the rest: leaving the documents stating there is no test runner is the drift Principle III forbids, one level up
- `scripts/verify.mjs` is named by the contracts' behaviour (tier sequencing, one overall verdict, exit statuses `0`/`1`/`2`/`3`) rather than by the plan's file tree, which lists only the npm scripts. It is the one file added here that the plan does not name
- Commit after each task or logical group; stop at any checkpoint to validate a story independently
